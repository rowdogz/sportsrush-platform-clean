{
  "name": "@sr/auth",
  "version": "0.0.1",
  "private": true,
  "description": "Auth utilities for the SportsRush platform — JWT, password hashing, role guards",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --ext .ts",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "jose": "^5.9.6"
  },
  "devDependencies": {
    "@sr/eslint-config": "workspace:*",
    "@sr/typescript-config": "workspace:*",
    "@sr/types": "workspace:*",
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  },
  "license": "UNLICENSED"
}
